version: 2

 
models:
  - name: fact_order
    columns:
      - name: order_id
        tests:
          - unique
          - not_null
  - name: fact_question
    columns:
      - name: question_id
        tests:
          - unique
          - not_null
